# Quaestio Bot (proprietary — view-only, see LICENSE.md)

Discord bot + pool worker + installers. Part of [Quaestio](https://quaestio.online).
Do not copy, host, or redistribute.

- `bot.py` — the Discord bot
- `quaestio.py` — manager CLI (`quaestio pool-serve`, `contribute`, …)
- `install.sh` / `install.ps1` — one-line installers
- `install_wizard.py` — full-screen setup TUI
